# .env.example
# Example environment variables for Sofware-AI
# -----------------------------
#
# ATTENTION:
# - The values below are placeholders and are NOT real values.
# - To use: Copy this file to `.env` and replace with real values.
# - Do not commit the `.env` file to git; .gitignore will ignore this file.
#
# How to use (PowerShell):
#   Copy-Item .env.example .env
#   # then edit .env with your editor to add real keys
#
# Example variables:

# Browser API key (replace with your actual key)
BROWSER_USE_API_KEY=bu_YOUR_BROWSER_USE_API_KEY_HERE

# GROQ API key
GROQ_API_KEY=gsk_YOUR_GROQ_API_KEY_HERE

# OpenAI API key (or your provider's key)
OPENAI_API_KEY=sk-YOUR_OPENAI_API_KEY_HERE

# Google API key (if using for models, maps, etc.)
GOOGLE_API_KEY=AIzaY0UR_GOOGLE_API_KEY_HERE

# Optional session key used by agents (if your application uses it)
SESSION_KEY=your_session_key_here

# Browser configuration: set to 1 or true for headless mode, 0/false for headed mode
BROWSER_HEADLESS=1

# Default model temperature (as decimal string)
MODEL_TEMPERATURE=0.5

# Other useful flags
# If you want to disable the startup banner in CLI, you can set:
DISABLE_BANNER=0

# NOTES:
# - If running on Windows and want to use ANSI colors,
#   libraries like colorama need to be initialized. This is related to the program code, not .env.
# - For secure key storage in CI/CD services, use your repository's Secrets/Variables
#   (e.g., GitHub Actions secrets) and do not include the .env file in the repository.
#
# If you add other services or keys, add them to this file in the same format.